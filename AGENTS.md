Think and write code and talk before calling commands in chinese only, reply to user in english

econome as many tokens as possible, dont overuse to check anything
use git add * instead of adding files independantly
when building or running commands with a lot of output make the command return only a few lines from the end
